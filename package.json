{
  "name": "jobs-2",
  "version": "0.0.0",
  "scripts": {
    "start": "vite --port 6060",
    "re:start": "rescript build -w",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build:gh": "tsc && vite build --base=/jobs-2/",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "preact": "^10.7.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.2.0",
    "concurrently": "^7.0.0",
    "gh-pages": "^3.2.3",
    "rescript": "^9.1.4",
    "typescript": "^4.6.3",
    "vite": "^2.9.1"
  }
}